# Используем базовый образ с поддержкой веб-сервера (например, Nginx)
FROM nginx

# Копируем содержимое вашего веб-сервера в образ
COPY . /usr/share/nginx/html

# Прописываем порт, на котором будет работать веб-сервер
EXPOSE 80
